# This config should fail because quux is not a valid subvol setting

subvol foo {
    mountpoint=/
    quux=bar
}

snap foo {
    subvol=foo
    dir=/.snapshots/yabsm/foo
    timeframes=5minute
    5minute_keep=1
}