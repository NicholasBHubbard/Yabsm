# This config should fail because there is no opening brace after foo_snap

subvol foo {
    mountpoint=/
}

snap foo_snap
    subvol=foo
    dir=/.snapshots/yabsm/foo
    timeframes=5minute
    5minute_keep=1
}