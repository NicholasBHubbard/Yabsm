# This config should fail because 32 is not an integer in range 1-31

subvol foo {
    mountpoint=/
}

snap foo {
    monthly_day=32
    timeframes=monthly
    dir=/foo
    subvol=foo
    monthly_keep=1
    monthly_time=23:59
}