# This config should fail because 1:59 is not a valid hh:mm time

subvol foo {
    mountpoint=/
}

snap foo {
     daily_time=1:59
     subvol=foo
     timeframes=daily
     dir=/foo
     daily_keep=1
}