#
# this config shows that we cannot parse a subvol missing
# an opening curly brace
#

yabsm_dir=/#always required

subvol home # missing curly brace

    mountpoint=/home

    5minute_want=no
    5minute_keep=0

    hourly_want=no
    hourly_keep=0

    midnight_want=no
    midnight_keep=0

    monthly_want=no
    monthly_keep=0
}
