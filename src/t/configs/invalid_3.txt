#
# This config is invalid because is is missing the root_hourly_take field. It
# is important to note that even though the root_hourly_want field is
# set to no, the other hourly fields must still be set to some value.
#

snapshot_directory=/

define_subvol=root

root_path=/

root_hourly_want=no
root_hourly_take= # NOT DEFINED
root_hourly_keep=0

root_daily_want=no
root_daily_take=0
root_daily_keep=0

root_midnight_want=no
root_midnight_keep=0 

root_monthly_want=no
root_monthly_keep=0
