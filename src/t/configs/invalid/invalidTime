#
# This config shows that we reject invalid times
#

yabsm_dir=/

subvol home {
    daily_want=yes
    daily_keep=100
    daily_time=24:60 # INVALID TIME

    mountpoint=/home
    5minute_want=no
    hourly_want=no
    weekly_want=no
    monthly_want=no
}