#
# This config shows we cannot backup an undefined subvol
# define backup block.
#

yabsm_dir=/#always required

backup someBackup {
    subvol=notASubvol
    remote=no
    backup_dir=/
    timeframe=hourly
    keep=100
}