# This config should fail because foo_snap is snapping
# an undefined subvol

subvol foo {
    mountpoint=/
}

snap foo_snap {
    subvol=bar
    dir=/.snapshots/yabsm/bar
    timeframes=5minute
    5minute_keep=1
}
