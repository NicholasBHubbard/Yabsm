# This config should fail because quux is not a valid week day

subvol foo {
    mountpoint=/
}

snap foo {
    weekly_day=quux
    timeframes=weekly
    subvol=foo
    weekly_keep=1
    weekly_time=23:59
    dir=/.snapshots/yabsm/foo
}
