#
# This config shows we cannot omit a opening curly brace in a
# define subvol block.
#

yabsm_dir=/#always required

subvol home #missing opening brace
    mountpoint=/home
    5minute_want=no
    hourly_want=no
    midnight_want=no
    weekly_want=no
    monthly_want=no
}