# This config should fail because quux is not a valid snap setting

subvol foo {
    mountpoint=/
}

snap foo {
    quux=bar
    subvol=foo
    dir=/.snapshots/yabsm/foo
    timeframes=5minute
    5minute_keep=1
}