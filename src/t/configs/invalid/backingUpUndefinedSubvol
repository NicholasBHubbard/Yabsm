#
# This config shows we cannot backup an undefined subvol
#

yabsm_dir=/#always required

backup someBackup {
    subvol=notASubvol
    remote=no
    backup_dir=/
    timeframe=5minute
    keep=100
}